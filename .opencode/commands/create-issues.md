---
description: Convert tasks/initial-issues.md into GitHub issues manually or with gh CLI after confirmation
agent: build
---

Read `tasks/initial-issues.md`.

Prepare GitHub issue creation commands, but do not execute them until the user explicitly approves.

For each issue, include:

- title
- labels
- body
- acceptance criteria

If `gh` is not authenticated, provide manual copy/paste issue bodies instead.
